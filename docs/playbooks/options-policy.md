# Options Policy

Options require explicit manual approval on every trade.

## Default block

Options are blocked in Phase 1 without explicit manual approval. Stocks and ETFs only on the allowed ticker list.
